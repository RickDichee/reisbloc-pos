rules_version = '2';

// Storage rules for TPV Solutions
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir lectura/escritura solo a usuarios autenticados
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
